<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"

        android:theme="@style/Theme.Smartfit2">

        <!-- 🔹 SplashActivity ko launcher activity banayenge -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 🔹 LoginActivity (Splash ke baad open hoga) -->
        <activity
            android:name=".LoginActivity"
            android:exported="false" />

        <!-- 🔹 SignupActivity -->
        <activity
            android:name=".SignupActivity"
            android:exported="false" />

        <!-- 🔹 MainActivity -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <activity android:name=".ChestActivity" />
        <activity android:name=".AbsActivity"/>
        <activity android:name=".LegsActivity"/>
        <activity android:name=".ArmsActivity" />
        <activity android:name=".BackActivity" />
        <activity android:name=".MuscleGainDietActivity"/>
        <activity android:name=".WeightLossDietActivity"/>
        <activity android:name=".BalancedDietActivity"/>

    </application>
</manifest>
