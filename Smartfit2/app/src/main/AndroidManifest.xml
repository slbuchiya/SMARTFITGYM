<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Smartfit2">

        <!-- 🔹 LoginActivity abhi sabse pehle open hoga -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 🔹 SignupActivity (login page se navigate karenge) -->
        <activity
            android:name=".SignupActivity"
            android:exported="false" />

        <!-- 🔹 MainActivity normal activity (login ke baad navigate karenge) -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />
        <activity android:name=".ChestActivity" />
        <activity android:name=".AbsActivity"/>
        <activity android:name=".LegsActivity"/>
        <activity android:name=".ArmsActivity" />
        <activity android:name=".BackActivity" />

    </application>
</manifest>
